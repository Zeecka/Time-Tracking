services:
  mariadb:
    image: mariadb:11.2
    container_name: record_db
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - mariadb_data:/var/lib/mysql
    networks:
      - record_network
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: record:backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: record_backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      FLASK_APP: app
      FLASK_ENV: production
      DATABASE_URL: mysql+pymysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mariadb:3306/${MYSQL_DATABASE}
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      - record_network

  frontend:
    image: record:frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: /api/v1
    container_name: record_frontend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - record_network

volumes:
  mariadb_data:

networks:
  record_network:
    driver: bridge
